# PayMix

AI 카드 배치 최적화 서비스 — 이 저장소에서 작업할 때 지켜야 할 설계 원칙.

- 상세 기획: [`PLANNING.md`](PLANNING.md)
- 서비스 소개 · 실행 방법: [`README.md`](README.md)

---

## 한 줄 정의

간편결제 경유로 **업종 판정에 실패해 받지 못한 카드 혜택**을 찾아내고,
보유 중인 **자사 카드**를 어느 결제처에 배치해야 혜택이 최대가 되는지 제안한다.
카드사에는 채널별 포지션과 혜택 정책 시뮬레이션을 제공한다.

## 두 개의 목적

- 소비자 → **똑똑한 소비**: 카드를 더 발급받는 게 아니라 이미 가진 카드를 제대로 쓰게 만든다
- 카드사 → **페이와의 상생**: 간편결제를 배제하지 않고 그 채널 안에서 선택받는 구조로 전환한다

## 비즈니스 모델

**B2B2C** — PayMix(공급) → 카드사(구매·운영) → 카드사의 고객(사용).
화면 6·7·8은 구매자인 카드사를 위한 관리자 도구이고, 2·3·4·5가 최종 사용자용이다.

---

## 액터

| Actor | 접근 | 역할 |
|---|---|---|
| 고객 | 로그인 | 카드 연결, 진단 조회, 개선 제안 확인 |
| 카드사 담당자 | 로그인 | 채널 포지션 분석, 정책 시뮬레이션, 세그먼트 추출, 혜택 규칙 관리 |
| 결제 승인계 (외부) | API | 거래 데이터 송신 — **인터페이스만 정의, 구현 제외** |

---

## 절대 어기지 말 것

1. **제안 범위는 자사 카드로 한정.**
   타사 카드 비교는 범위 밖이다. 고객이 A카드사 앱에서 "B카드사가 낫다"는 답을 받으면
   서비스가 성립하지 않는다.
   자사 카탈로그의 미보유 상품은 **연회비를 차감하고도 순증이 남는 경우에만** 참고로 제시한다.

2. **모든 혜택 수치는 서버 규칙 엔진이 산출.**
   LLM은 확정된 수치를 자연어로 설명하는 역할만. 금융 도메인이므로 수치 생성을
   생성형 모델에 위임하지 않는다.

3. **채널 정책은 (카드 × 채널) 교차 속성.**
   카드 속성도 채널 속성도 아니므로 `card_channel_rates` 별도 엔터티로 분리한다.
   두 축을 함께 관리한다 — `performance_rate`(실적 인정률)와
   **`is_benefit_eligible`(업종 할인 적용 여부)**. 후자가 실무에서 실제로 갈리는 축이다.

4. **제안 상세는 계산 시점 스냅샷으로 저장.**
   혜택 규칙은 변경되므로 규칙 참조로 저장하면 과거 제안의 근거가 소실된다.

5. **집계는 저장하지 않는다.**
   화면 6 대시보드 지표와 화면 7 시뮬레이션 결과는 거래에서 실시간 산출한다.
   그래서 이력 조회 API도 UI 메뉴도 두지 않는다 — *UI에 없는 기능은 API에도 두지 않는다.*

6. **화면에 개발자 용어를 노출하지 않는다.**
   상태 코드·필드 경로·엔터티명은 사용자에게 보이지 않는다. 사용자에게는 `message`만 보여준다.

---

## 손실은 두 종류다 — 섞으면 안 된다

| 유형 | 원인 | 카드를 바꾸면 | 데이터 근거 |
|---|---|---|---|
| **약관상 실적 제외** | 통신비·공과금·구독료·선불충전 | 달라지지 않음 | `spending_categories.default_performance_rate = 0` |
| **업종 할인 미적용** | 간편결제 경유로 업종 판정 불가 | **전액 회복** | `card_channel_rates.is_benefit_eligible = false` |

간편결제라고 **실적에서 빼는 정책은 드물다.** 카드사 입장에선 페이 경유도 자사 카드 결제이고
수수료도 자사 몫이다. 실제로 갈리는 것은 **업종별 할인 적용 여부**다.

---

## 서비스 흐름

```
[1] 카드 데이터 연결
      ↓
[2] 현황 진단
      ├ 실적·연회비: 인정 실적, 약관상 제외, 업종 할인 미적용, 연간 순이익
      └ 페이·정기결제: 채널별 결제 비중, 정기결제별 실적 인정 여부
      ↓
[3] 개선 제안
      결제처별 권장 카드 배치, 정기결제 이전, 항목별 혜택 활용도,
      자사 신규 카드 추천, 실적 구간 근접 안내
```

## 핵심 산출 로직

```
① 인정실적 = Σ (결제액 × 카테고리인정률 × 채널인정률)
② 실적 구간 판정 → 적용 혜택 세트 확정
③ 실효혜택 = min(Σ min(결제액 × 할인율, 항목월한도), 통합월한도)
④ 연간 순이익 = ((실효혜택 + 채널적립) × 12) − 연회비
⑤ 최적 배치: (채널 × 카테고리) 단위 배분 중 순이익 최대
```

구현은 [`frontend/src/api/mock/engine.js`](frontend/src/api/mock/engine.js).
하드코딩된 수치 없이 거래 데이터에서 매번 계산한다.

---

## 화면 8개

| # | 액터 | 화면 |
|---|---|---|
| 1 | 공통 | 로그인 / 회원가입 |
| 2 | 고객 | 카드 데이터 연결 |
| 3 | 고객 | 실적·연회비 진단 |
| 4 | 고객 | 페이·정기결제 현황 |
| 5 | 고객 | 개선 제안 |
| 6 | 카드사 | 채널 포지션 대시보드 |
| 7 | 카드사 | 혜택 정책 시뮬레이션 |
| 8 | 카드사 | 카드·혜택 규칙 관리 |

### 예외 시나리오 (반드시 유지)

- 화면 3: 거래 데이터 부족(가입 1개월 미만) → `422` 진단 불가 안내
- 화면 5: 현재 배치가 이미 최적 → **오류가 아닌 `201` 정상 응답**, `status = NO_CHANGE_NEEDED`
- 화면 7: 시뮬레이션 파라미터 범위 초과 → `400` + 실패 필드 경로 반환

---

## 엔티티 14개

```
users                   계정 (고객 / 카드사 담당자)
customers               고객 프로필 — joined_at 이 진단 가능 여부 판정 기준
cards                   카드 상품
customer_cards          보유 카드 — 최적 배치 탐색의 후보 집합
spending_categories     소비 카테고리 — default_performance_rate 0 이 약관상 제외
pay_channels            결제 채널 (실물, 삼성페이, 네이버페이, 카카오페이 …)
card_performance_tiers  카드별 실적 구간
benefit_rules           혜택 규칙 (카드 × 카테고리 × 구간, 3종 한도)
card_channel_rates      카드 × 채널 교차 — 인정률 + 업종 할인 적용 여부
transactions            거래 내역 — 전 계산의 원천, external_tx_id 멱등성 키
subscriptions           정기결제
point_balances          포인트 잔액·소멸 예정
recommendations         제안 실행 이력
recommendation_details  차분 상세 — snapshot_* 로 계산 시점 보관
```

---

## 코드 구조

```
frontend/src/api/
  client.js     MODE='mock' | 'http' 스위치. 두 모드의 요청/응답은 OAS 로 동일
  index.js      41개 엔드포인트 함수 — OAS operationId 와 1:1
  mock/
    seed.js       DBML 14 테이블 시드 데이터
    engine.js     규칙 엔진 (산출 로직 ①~⑤ + 카테고리 활용도 + 신규 카드 추천)
    handlers.js   41개 오퍼레이션 구현 + 에러 응답
```

산출물 3종은 [`deliverables/`](deliverables/) 에 있고, 셋은 항상 서로 일치해야 한다.
엔진 계산값을 바꾸면 기술서의 예시 수치도 함께 갱신한다.
