# PayMix

간편결제 시대의 카드 최적 배치 서비스 (SKALA 3기 AI 웹 서비스 설계 Mini-project)

- 담당: 3반 P074 박수빈
- 상세 기획: `docs/PLANNING.md`
- 마감: 3일차 14:00 제출 / 14:30 발표 (개인별 5분)

---

## 한 줄 정의

카드사 내부 결제 데이터를 기반으로 고객의 실적·혜택·정기결제 현황을 진단하고,
보유 중인 **자사 카드**를 어느 간편결제 채널에 배치해야 혜택이 최대가 되는지 제안한다.
카드사에는 채널별 포지션과 혜택 정책 시뮬레이션을 제공한다.

## 두 개의 목적

- 소비자 → **똑똑한 소비**: 카드를 더 발급받는 게 아니라 이미 가진 카드를 제대로 쓰게 만든다
- 카드사 → **페이와의 상생**: 간편결제를 배제하지 않고 그 채널 안에서 선택받는 구조로 전환한다

---

## 액터

| Actor | 접근 | 역할 |
|---|---|---|
| 고객 | 로그인 | 카드 연결, 진단 조회, 개선 제안 확인 |
| 카드사 담당자 | 로그인 | 채널 포지션 분석, 정책 시뮬레이션, 세그먼트 추출, 혜택 규칙 관리 |
| 결제 승인계 (외부) | API | 거래 데이터 송신 — **인터페이스만 정의, 구현 제외** |

---

## 절대 어기지 말 것 (설계 원칙)

1. **제안 범위는 자사 보유 카드 내 재배치로 한정.**
   타사 카드 비교나 신규 발급 유도는 범위 밖. (고객이 A카드사 앱에서 "B카드사가 낫다"는
   답을 받으면 서비스가 성립하지 않음)

2. **모든 혜택 수치는 서버 규칙 엔진이 산출.**
   LLM은 확정된 수치를 자연어로 설명하는 역할만. 금융 도메인이므로 수치 생성을
   생성형 모델에 위임하지 않는다.

3. **채널 인정률은 (카드 × 채널) 교차 속성.**
   카드 속성도 채널 속성도 아니므로 `card_channel_rates` 별도 엔티티로 분리.
   이 판단이 카드사별 채널 차등 정책과 시뮬레이션 기능의 구현 가능 여부를 결정한다.

4. **제안 상세는 계산 시점 스냅샷으로 저장.**
   혜택 규칙은 변경되므로 규칙 참조로 저장하면 과거 제안의 근거가 소실된다.

---

## 서비스 흐름

```
[1] 카드 데이터 연결
      ↓
[2] 현황 진단
      ├ 실적·연회비: 인정 실적, 제외 내역, 포인트, 연간 순이익
      └ 페이·정기결제: 채널별 결제 비중, 정기결제별 실적 인정 여부
      ↓
[3] 개선 제안
      페이별 권장 카드 배치, 정기결제 이전, 실적 구간 근접 안내
```

## 핵심 산출 로직

```
① 인정실적 = Σ (결제액 × 카테고리인정률 × 채널인정률)
     채널인정률: 실물 100%, 간편결제는 카드사 정책에 따라 0~100%
     정기결제는 카테고리인정률이 0인 경우가 다수
② 실적 구간 판정 → 적용 혜택 세트 확정
③ 실효혜택 = min(Σ min(결제액 × 할인율, 항목월한도), 통합월한도)
④ 연간 순이익 = ((실효혜택 + 채널적립) × 12) − 연회비
⑤ 최적 배치: (페이채널 × 보유카드), (정기결제 × 보유카드) 조합 중 순이익 최대 탐색
```

---

## 화면 8개

| # | 액터 | 화면 |
|---|---|---|
| 1 | 공통 | 로그인 / 회원가입 |
| 2 | 고객 | 카드 데이터 연결 |
| 3 | 고객 | 실적·연회비 진단 |
| 4 | 고객 | 페이·정기결제 현황 |
| 5 | 고객 | 개선 제안 |
| 6 | 카드사 | 채널 포지션 대시보드 |
| 7 | 카드사 | 혜택 정책 시뮬레이션 |
| 8 | 카드사 | 카드·혜택 규칙 관리 |

### 예외 시나리오 (평가 가점 항목 — 반드시 포함)

- 화면 5: 현재 배치가 이미 최적 → "변경 실익 없음" 처리
- 화면 3: 거래 데이터 부족(가입 1개월 미만) → 진단 불가 안내
- 화면 7: 시뮬레이션 파라미터 범위 초과 → 검증 오류

---

## 엔티티 11개

```
users                   계정 (고객 / 카드사 담당자)
customers               고객 프로필
cards                   카드 상품
customer_cards          보유 카드
spending_categories     소비 카테고리
pay_channels            결제 채널 (실물, 삼성페이, 네이버페이, 카카오페이 …)
benefit_rules           혜택 규칙 (카테고리별 할인율, 3종 한도, 실적 구간)
card_channel_rates      카드 × 채널 인정률 (교차 엔티티)
subscriptions           정기결제
recommendations         제안 실행 이력
recommendation_details  카테고리·채널별 차분 (계산 시점 스냅샷)
```

---

## 산출물 3종 (파일명 고정)

```
3반_P074_박수빈_PayMix-개요.pdf    서비스 개요·배경·액터·UI 흐름도
3반_P074_박수빈_PayMix-DB.dbml     데이터 모델
3반_P074_박수빈_PayMix-API.yml     OAS 3.0 API 명세
```

**UI 흐름도는 반드시 PDF 파일 내에 포함되어야 함 (화면 캡처 붙여넣기)**

---

## 평가 기준 요약 (100점)

| 항목 | 배점 | 핵심 |
|---|---|---|
| 납기 | 20 | 3일차 14:00 — **미준수 0점** |
| 목표 시스템 정의 | 30 | 배경·액터별 기능·**전체 화면 와이어프레임** |
| 데이터 모델 | 20 | UI에 나오는 모든 필드가 ERD에 존재 |
| API 명세 | 20 | 화면에서 호출되는 API 전체, 에러 코드(400/401/404), `$ref` 재사용 |
| 발표 | 10 | 5분, 시간 엄수 |

- **창의/독창성은 평가 항목이 아님.** 구현 및 기능 명확성으로 평가
- 감점 1순위는 **UI ↔ ERD ↔ API 3자 불일치**
- Security Schemes는 평가 제외 — 인증 스키마에 시간 쓰지 말 것
- "과도하게 큰 범위(비현실적 서비스)"도 명시적 감점 사유

---

## 작업 순서

1. 와이어프레임 8장 (draw.io / Excalidraw — 정교할 필요 없음)
2. DBML 작성 (dbdiagram.io) — **ERD가 먼저 굳어야 API 스키마를 `$ref`로 재사용 가능**
3. OAS YML 작성 (Swagger Editor)
4. 정합성 검수 — 화면 × API × 엔티티 매트릭스로 빈칸 탐색

## 시간 부족 시 잘라낼 순서

1. 카드사 세그먼트 추출 기능
2. 포인트 잔액·만료 관리
3. 실적 구간 근접 알림

**절대 자르면 안 되는 것**: 와이어프레임 전체, API 에러 코드 정의
